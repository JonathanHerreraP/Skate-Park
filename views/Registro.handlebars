<!DOCTYPE html>
<html lang="en">
  <head>
  
  </head>
  <body>
    <h1>Skate Park</h1>

    <div class="py-4">
      <h2>Registro</h2>
      <hr class="w-50" />

      <form method="POST" enctype="multipart/form-data">
        <div class="form-group row w-50 m-auto">
          <div class="form-group col-12 col-sm-6">
            <label>Email</label>
            <input id = "email" class="form-control m-auto" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Nombre</label>
            <input id = "nombre" class="form-control m-auto" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Password</label>
            <input type="password" id = "password" class="form-control m-auto" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Repita la password</label>
            <input type="password" id = "repitapassword" class="form-control m-auto" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Años de experiencia</label>
            <input id = "años" class="form-control m-auto" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Especialidad</label>
            <input id = "especialidad" class="form-control m-auto" />
          </div>
          <div class="form-group col-12 col-sm-12">
            <label>Foto de perfil</label>
            <input id = "foto" type="file" name="fotito" />
          </div>
        </div>
        <button id = "boton1" class="btn btn-info mb-3">Registrarme</button>
        <p><a href="Login"> Iniciar sesión</a></p>
      </form>
    </div>
  </body>

  <script>
    // Capturamos con Jquery los inputs del formulario registro:
    $("#boton1").click(async ()=>{

      const email = $("#email").val();
      const nombre = $("#nombre").val();
      const password = $("#password").val();
      const repitapassword = $("#repitapassword").val();
      const anos_experiencia = $("#años").val();
      const especialidad = $("#especialidad").val();
      const foto = $("#foto").val();
      const foto2 = $("#foto").val
    

    const payload = {email, nombre, password, anos_experiencia, especialidad, foto}
    console.log(payload)

    try{
      await axios.post("/Registro", payload);
       alert("Usuario registrado exitosamente")
      window.location.href = "Login"
    }
    catch(e){
    console.log("error:" + e);
    }
    });
  </script>

</html>
